# Draco Dataset Studio — Backend Requirements
# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Database
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
alembic>=1.14.0

# Config
pydantic-settings>=2.6.0

# Image processing (always required)
pillow>=11.0.0
imagehash>=4.3.1
numpy>=1.26.0
aiofiles>=24.1.0

# HTTP client (for API providers)
httpx>=0.28.0

# ── Optional: face detection ──────────────────────────────────────────────────
# insightface>=0.7.3   # Uncomment after manual install (requires cmake)
# onnxruntime>=1.20.0  # CPU-only ONNX runtime

# ── Optional: GPU ONNX (choose one) ───────────────────────────────────────────
# onnxruntime-gpu>=1.20.0  # CUDA-enabled ONNX runtime

# ── Optional: embeddings + vector search ─────────────────────────────────────
# fastembed>=0.3.0          # CLIP ONNX embeddings (no PyTorch)
# qdrant-client[local]>=1.12.0  # Embedded Qdrant

# ── Optional: ranking ────────────────────────────────────────────────────────
# openskill>=6.0.0

# ── Optional: captions (local) ───────────────────────────────────────────────
# transformers>=4.50.0
# torch>=2.6.0         # Required by Florence-2, JoyCaption
# accelerate>=1.0.0

# ── Optional: quality scoring ────────────────────────────────────────────────
# opencv-python-headless>=4.10.0

# ── Optional: background removal ─────────────────────────────────────────────
# rembg>=2.0.56

# ── Optional: dataset clustering ─────────────────────────────────────────────
# hdbscan>=0.8.38
# scikit-learn>=1.5.0
# umap-learn>=0.5.6

# ── Optional: API key encryption ─────────────────────────────────────────────
# cryptography>=43.0.0

# ── Optional: export formats ─────────────────────────────────────────────────
# tomli-w>=1.0.0   # TOML writing for Kohya config
