# DRACO Dataset Studio v5 — Python Dependencies
# GPU-accelerated AI pipeline for face analysis, captioning, image quality, and dataset curation

# Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Core
numpy>=1.26.0
opencv-python>=4.10.0
Pillow>=10.4.0

# Face Analysis (InsightFace ArcFace 512-d — 99.83% LFW accuracy)
insightface>=0.7.3
onnxruntime-gpu>=1.19.0; platform_system=="Windows"
onnxruntime>=1.19.0; platform_system!="Windows"

# CLIP + Aesthetic Scoring
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.46.0

# HuggingFace model downloads
huggingface-hub>=0.26.0

# Florence-2 captioning requires
timm>=1.0.0
einops>=0.8.0

# Image Quality Assessment — TOPIQ (NeurIPS 2024, state-of-the-art no-reference IQA)
# Also provides CLIP-IQA+, MUSIQ, NIMA, HyperIQA
pyiqa>=0.1.12

# Background Removal — BRIA RMBG-2.0 (best portrait extraction)
rembg>=2.0.59

# Near-Duplicate Detection (optional, falls back to pHash if unavailable)
# SSCD by Meta — learned copy detection, outperforms pHash for crops/edits
# Note: SSCD is loaded via torch.hub, no extra pip install needed

# ── v5 additions ────────────────────────────────────────────────────────────

# CLIP embeddings via fastembed (ONNX, no heavy torch dep, GPU via onnxruntime-gpu)
fastembed>=0.4.0

# Clustering + ML utilities
scikit-learn>=1.4.0     # DBSCAN for face identity clustering, normalize
hdbscan>=0.8.38         # HDBSCAN for image content clusters (no k required)

# Dimensionality reduction for cluster map
umap-learn>=0.5.6

# TrueSkill pairwise ranking (fewer comparisons than ELO, models uncertainty)
trueskill>=0.4.5

# DeepFace for face attribute analysis (emotion, age) — CPU mode only
deepface>=0.0.90
